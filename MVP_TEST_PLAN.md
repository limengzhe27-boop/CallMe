# CallMe 第一阶段真机实验

目标：验证免费 Personal Team 环境下，本地延迟触发 CallKit 能否在锁屏/后台产生足够明显的系统来电体验。

## 配置维度

每个关键场景至少分别测试：

- 手机来电
- 微信语音（CallKit 承接锁屏来电，接听后显示本地微信风格页）
- 微信视频（CallKit 标记视频能力，接听后显示本地视频风格页）
- 微信视频接听后启用本机摄像头小窗；本地相册视频作为可选的对方画面（静音循环）
- 不设置接听音频（静音）
- 自定义接听后音频
- 系统默认铃声
- CallMe 双音铃声

同时记录所选延迟、计划触发时间、实际触发时间和 App 日志中的毫秒偏差。

## 运行前准备

1. 在 Xcode 的 `Signing & Capabilities` 中选择自己的 Personal Team。
2. 选择已配对的 iPhone，安装并首次启动 App。
3. iPhone 保持非静音，铃声音量调高，并暂时关闭“勿扰模式/专注模式”。
4. 每轮测试前结束上一通模拟电话，确认 App 状态显示“尚未开始”或上一轮已结束。
5. 前台微信风格页过响时，使用“CallMe 应用内音量”调低；该滑块不会改变其他 App 的系统音量。

## 第一轮测试

每轮选择相应延迟并点击“模拟来电”，然后立刻进入对应状态。

| 场景 | 是否响铃/震动 | 是否亮屏 | 是否出现系统来电 UI | 是否可接听 | 是否可拒绝 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| App 保持前台 | 成功 | 不适用 | 手机使用系统页；微信使用各自模拟页 | 成功 | 成功 | 1.0 三模式分离后待复测 |
| 点击后返回主屏幕 | 待测试 | 待测试 | 待测试 | 待测试 | 待测试 | |
| 点击后立即锁屏 | 成功 | 成功 | 成功 | 成功 | 成功 | iPhone 16 / iOS 26.5 |
| 后台等待 10 秒 | 成功 | 成功 | 成功 | 成功 | 成功 | iOS 0.6 真机确认 |

## 第二轮时长实验

当前 UI 支持 10 秒、30 秒、1 分钟及 1 秒至 24 小时的自定义延迟。1 分钟以上仅用于
观察 iOS 后台挂起边界，不代表能够准时触发。

| 锁屏时长 | 是否触发 | 实际延迟 | App 是否疑似被挂起 | 备注 |
| --- | --- | --- | --- | --- |
| 10 秒 | 成功 | 约 10 秒，日志偏差约 0.1–0.5 秒 | 未发现 | iOS 0.6 |
| 30 秒 | 待测试 | 待测试 | 待测试 | |
| 1 分钟 | 待测试 | 待测试 | 待测试 | |
| 5 分钟 | 待测试 | 待测试 | 待测试 | 预期可能因 App 挂起而失败 |
| 30 分钟 | 待测试 | 待测试 | 待测试 | 记录是否在解锁后补触发 |
| 24 小时 | 待测试 | 待测试 | 待测试 | 仅验证边界，不作为可靠能力承诺 |

## 失败时记录

- iPhone 型号和 iOS 版本。
- App 当时处于前台、后台还是锁屏。
- 是否静音、是否开启专注模式。
- App 事件记录中是否出现 `开始 reportNewIncomingCall`。
- 是否出现 `reportNewIncomingCall 成功` 或具体错误。
- 从点击按钮到失败/触发大约经过多少秒。
- 每轮结束后从“设置 > 状态与诊断”复制完整诊断报告。
- 活动记录会跨 App 启动保留最近 200 条；确认报告已经复制后再清空，避免不同轮次混在一起。

普通通知不计为成功；成功必须至少验证明显响铃或震动、亮屏、系统来电 UI，以及接听/拒绝操作。
